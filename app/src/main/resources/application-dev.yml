spring:
  profiles: dev
  datasource:
      driverClassName: org.postgresql.Driver
      url: jdbc:postgresql://localhost:5432/iblog_dev
      username: user
      password: user
logging:
    config: classpath:logback-tomcat.xml